# SSR API target for server-rendered Node requests.
SERVER_API_URL=https://rupakar-backend.onrender.com/api/v1

# Browser-side requests must target the deployed backend API, not the Vercel frontend origin.
NEXT_PUBLIC_API_URL=https://rupakar-backend.onrender.com/api/v1
